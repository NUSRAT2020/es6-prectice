// if u want to give space for separate  word 
const firstName =  "justin" ;
const lastName = "viber"
const fullName = firstName + lastName
const fullName2 = `${firstName} ${lastName} is a good man ${100+200-111/12*5}`
console.log(fullName2);

// `` thisz sign also separate the line 
 const multiLine = `asddds
 sadfrf
 asd
 dfghg`
 console.log(multiLine);